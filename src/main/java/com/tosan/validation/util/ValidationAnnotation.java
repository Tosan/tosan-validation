package com.tosan.validation.util;

/**
 * Name of ValidationAnnotation enum value must be the same as the annotation name.
 *
 * @author Zahra Hajihosseinkhani
 * @since 1/11/15
 */
public enum ValidationAnnotation {

    COMPARE,
    DIFFERENCE,
    DATEDIFFERENCE,
    DIGITS,
    EMAIL,
    EXPRESSION,
    FUTUREDATE,
    GROUP,
    LENGTH,
    MIN,
    MAX,
    NOTEMPTY,
    PASTDATE,
    PATTERN,
    SIZE,
    NOTNULL,
    OTHER
}
